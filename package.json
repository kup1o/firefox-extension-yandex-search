{
  "name": "yandex-search-web-ext",
  "version": "1",
  "description": "Yandex search engine for web-ext",
  "scripts": {
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest -a ./dist -s ./src",
    "run": "web-ext run"
  },
  "keywords": [
    "firefox",
    "extension",
    "yandex",
    "search"
  ],
  "author": "kup1o",
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^7.11.0"
  },
  "engines": {
    "node": ">=12"
  }
}
